import { InitAppChatDirective } from './init-app-chat.directive';

describe('InitAppChatDirective', () => {
  it('should create an instance', () => {
    const directive = new InitAppChatDirective();
    expect(directive).toBeTruthy();
  });
});
