import { SafeUrlDirective } from './safe-url.directive';

describe('SafeUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new SafeUrlDirective();
    expect(directive).toBeTruthy();
  });
});
