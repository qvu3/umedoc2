import { InitPatientMenuDirective } from './init-patient-menu.directive';

describe('InitPatientMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new InitPatientMenuDirective();
    expect(directive).toBeTruthy();
  });
});
